装饰模式
1.动态的给一个对象添加一些额外的职责，就增加功能来说，装饰模式 比生成子类更为灵活。
2.装饰模式是为已有功能动态的添加更多功能的一种方式，当系统需要新功能的时候，是想旧的类中添加新代码。
	这些新加的代码通过装饰了原有类的核心职责或主要行为，他们在主类中加入了新的字段，新的方法和新的逻辑，从而增加了主类的复杂度，
	而这些新加入的东西仅仅是为了满足一些只有在某种特定情况下才会执行的特殊行为的需要。
	而装饰模式却提供了一个非常好的解决方案，他把每个要装饰的功能放在单独的类中，并让这个类包装他所需要装饰的对象，
	因此，当需要执行特殊行为时，客户代码就可以在运行时根据需要有选择的，按顺序的使用装饰功能包装对象了。
3.装饰模式优点
	a.把类中的装饰功能从类中搬移除去，这样可以假话原有的类
	b.有效的把类的核心职责和装饰功能区分开了，而且可以去除相关类中重复的装饰逻辑。

注意
使用装饰模式，在添加装饰的时候，需要注意添加顺序

说明：
demo包 对应第一个案例
demo2包 对应第二个案例
demo3包 讲解装饰模式结构，与装饰模式结构图一一对应
demo4包是将demo包中的需求转换为装饰模式来使用	